#!/bin/bash
set -eu # exit script on errors

echo "copy qr-hunt env"
cp /home/git/www/qr-hunt/.env .

echo "> npm install..."
npm install

echo "> npm run build..."
npm run build

export MAIN="server"
